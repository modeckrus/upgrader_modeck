name: upgrader_example
description: Demonstrates how to use the upgrader_modeck package.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  upgrader_modeck:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
